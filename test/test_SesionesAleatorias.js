var assert = require('assert');

import SesionEstudio from "../src/clases/SesionEstudio.js";
import FuncionesAuxiliares from "../src/clases/FuncionesAuxiliares.js";
import ColeccionSesiones from "../src/clases/ColeccionSesiones.js";

describe('Probando Sesiones Aleatorias', function () {

    // TODO Formalizar test de sesiones aleatorias

    /*
    let cs = null;
    let s = null;

    console.log("Sesiones aleatorias");
    cs = new ColeccionSesiones();
    for(var i=0; i<10; i++) {
        s = SesionEstudio.newAleatoria();
        console.log(s.getMinutosEstudio());
        cs.addSesion(s);
    }
    for(var i=0; i<cs.getSesiones().length; i++) {
        console.log(cs.getSesiones()[i].getMinutosEstudio());
    }
    
    console.log("Sesiones aleatorias en un rango");
    cs = new ColeccionSesiones();
    for(var i=0; i<10; i++) {
        s = SesionEstudio.newAleatoriaRango(new Date("2020-06-01 00:00:00"), new Date("2020-06-30 23:59:59"), 60, 180);
        console.log(s.getMinutosEstudio());
        cs.addSesion(s);
    }
    for(var i=0; i<cs.getSesiones().length; i++) {
        console.log(cs.getSesiones()[i].getMinutosEstudio());
    }    
    */

});